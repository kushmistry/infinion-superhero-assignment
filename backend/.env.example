# ===========================================
# SUPERHERO HUB - BACKEND ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# Database Configuration
# Get this from your Neon DB dashboard: https://neon.tech
# Format: postgresql://username:password@host/database?sslmode=require
DATABASE_URL=postgresql://your_username:your_password@your_host/your_database?sslmode=require

# JWT Authentication (for user login sessions)
# Generate a secure random string for production
SECRET_KEY=your_jwt_secret_key_here_generate_a_strong_random_string

# Superhero API Configuration
# Get your free token from: https://www.superheroapi.com/
SUPERHERO_API_TOKEN=your_superhero_api_token_here

# CORS Configuration (comma-separated URLs)
# URLs that are allowed to access the backend API
BACKEND_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Frontend Configuration
# URL of your frontend application for password reset links
FRONTEND_BASE_URL=http://localhost:3000

# SMTP Email Configuration (for password reset emails)
# For Gmail: Enable 2FA, generate App Password at https://myaccount.google.com/apppasswords
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here
FROM_EMAIL=your_email@gmail.com
SMTP_USE_TLS=true
